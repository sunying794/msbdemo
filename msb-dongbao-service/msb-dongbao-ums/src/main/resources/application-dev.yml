server:
  port: 8080

spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/mysql?serverTimezone=GMT&useUnicode=true&useSSL=false&characterEncoding=utf8
    driver-class-name: com.mysql.cj.jdbc.Driver

#mybatis:
#  mapper-locations: classpath:/com/mapper/*.xml
#  type-aliases-package: com.entity

#showSql
logging:
  level:
    com:
      example:
        mapper : debug